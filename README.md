# Deep Research Agent

Персональный агент глубокого исследования с human-in-the-loop.

## Быстрый старт

```bash
# Установка
pip install -e .

# Настройка (скопируйте config.toml и добавьте API ключи)
cp config/config.example.toml config/config.toml

# Запуск исследования
deep-research query "transformer architectures for time series"
```

## Особенности

- 🔍 **Интеллектуальный поиск** — автоматический поиск через Tavily, GitHub, arXiv
- 👤 **Human-in-the-loop** — контроль на каждом шаге исследования
- 🧠 **Память** — SQLite persistence между сессиями
- 📝 **Структурированные отчеты** — JSON + Markdown
- 🎯 **Простота** — работает из коробки

## Архитектура

Проект использует LangGraph для оркестрации stateful агентов:

```
Plan → Search → Analyze → Report
      ↑              ↓
      └──── Human ───┘
```

## Концепция

См. `docs/CONCEPT.md` в этом репозитории.

## Лицензия

MIT
